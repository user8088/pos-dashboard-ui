// Import the existing CustomerProfile component
import CustomerProfile from "views/Dashboard/CustomerProfile";

const FactoryCustomerProfile = () => {
  // Use the existing CustomerProfile component
  return <CustomerProfile />;
};

export default FactoryCustomerProfile;
